{
  "name": "yup-endpoints-server",
  "description": "A server which uses Yup to validate the incoming data.",
  "author": "Jack Scott",
  "version": "0.5.1",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "readme.md"
  ],
  "keywords": [],
  "repository": {
    "type": "git",
    "url": "https://github.com/jackrobertscott/yup-endpoints-server.git"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist/*",
    "dev": "ts-node-dev src/index.ts",
    "prepublishOnly": "npm run test && npm run clean && npm run build",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.7",
    "@types/node": "^20.8.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  },
  "peerDependencies": {
    "yup": "^1.3.2"
  },
  "dependencies": {
    "yup-endpoints": "^0.4.0"
  }
}
